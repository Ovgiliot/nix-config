# Game Development home workflow — engines and toolchains.
{pkgs, ...}: {
  home.packages = with pkgs; [
    # TODO: unreal-engine (not in nixpkgs — requires manual packaging)
    # TODO: unity (not in nixpkgs — requires manual packaging)
  ];
}
