# NixOS Dotfiles

A modular, multi-profile NixOS and macOS configuration using **Nix Flakes** and **Home Manager**, following the Suckless philosophy: minimalism, clarity, and explicit configuration over magic defaults.

**Desktop:** Niri (scrollable-tiling Wayland) + QuickShell (status bar) + Wofi + Mako
**Terminal:** Ghostty | **Editor:** Neovim | **Shell:** Fish
**Input:** Kanata home-row mods | **Audio:** PipeWire
**Theming:** Matugen (Material Design 3 wallpaper-based colors)

---

## Project Structure

```
.
├── flake.nix               # Entry point — mkNixosHost / mkDarwinHost helpers, checks, formatter
│
├── profiles/               # Opinionated machine archetypes (import a profile, not individual modules)
│   ├── laptop.nix          # Core + desktop + ThinkPad power management + virtualization + swww wallpaper
│   ├── workstation.nix     # Core + desktop + gaming + virtualization (no power management)
│   ├── server.nix          # Core only — headless, no GUI
│   └── darwin.nix          # nix-darwin + HM: core + macOS overrides (no desktop)
│
├── hosts/                  # One directory per machine
│   ├── nixos/              # ThinkPad #1 (ext4, Kaby Lake, Thunderbolt)
│   │   ├── default.nix     # { inputs }: { system, specialArgs, modules }
│   │   └── hardware.nix    # Generated by nixos-generate-config (do not edit)
│   └── nixpad/             # ThinkPad #2 (btrfs, separate /home)
│       ├── default.nix
│       └── hardware.nix
│
├── modules/
│   ├── system/             # NixOS system modules — one concern per file
│   │   ├── core/
│   │   │   ├── boot.nix        # Shared UEFI boot (systemd-boot, TPM2)
│   │   │   ├── nix.nix         # Flakes, substituters, weekly GC
│   │   │   ├── locale.nix      # Timezone (Kyiv), system language, Ukrainian formats
│   │   │   ├── networking.nix  # NetworkManager, nftables firewall, random WiFi MAC
│   │   │   └── security.nix    # Kernel hardening, AppArmor, sysctl, network hardening
│   │   ├── desktop/
│   │   │   ├── audio.nix       # PipeWire
│   │   │   ├── display.nix     # Niri, greetd/tuigreet, XDG portal, fonts
│   │   │   └── input.nix       # Kanata service, uinput udev rules, XKB us,ru
│   │   ├── laptop/
│   │   │   ├── boot.nix        # systemd-boot, Zen kernel, systemd initrd, LUKS (swap)
│   │   │   ├── power.nix       # power-profiles-daemon, thermald, thinkfan, hibernate
│   │   │   └── services.nix    # Bluetooth, fwupd, fstrim, zram
│   │   └── optional/
│   │       ├── gaming.nix          # Steam, Gamescope, Gamemode, zram tuning
│   │       └── virtualization.nix  # QEMU/KVM, libvirtd, virt-manager, SPICE
│   │
│   └── home/               # Home Manager modules — imported by profiles via home-manager.users.ethel.imports
│       ├── core/
│       │   ├── default.nix       # Aggregates sub-modules; git/gh identity, ranger + opencode XDG links
│       │   ├── shell.nix         # Fish + Bash aliases
│       │   ├── packages.nix      # CLI tools, fonts, dev stack, LSPs, formatters
│       │   ├── neovim.nix        # Neovim + full plugin set (treesitter, LSP, DAP, copilot, orgmode, etc.)
│       │   ├── keymap.nix        # Centralized keymap generator (Neovim langmap + qutebrowser keymap)
│       │   └── keymap-layouts.nix  # Pure data: non-Latin→Latin character mappings + Qt key codes
│       ├── desktop/
│       │   ├── default.nix       # Aggregates all desktop HM sub-modules
│       │   ├── theme.nix         # GTK/Qt theming (adw-gtk3-dark), dconf dark preference, swaylock-effects
│       │   ├── niri.nix          # Niri config directory XDG link
│       │   ├── quickshell.nix    # QuickShell bar: QML bundle via runCommand, helper scripts, systemd service
│       │   ├── ghostty.nix       # Ghostty config + shaders XDG links
│       │   ├── notifications.nix # Stub — mako config managed entirely by matugen
│       │   ├── launcher.nix      # Wofi config XDG link (style managed by matugen)
│       │   ├── matugen.nix       # Matugen package + config/templates XDG links + activation hook
│       │   ├── apps.nix          # Desktop packages (Wayland utils, Ghostty, etc.) + imports web-apps.nix
│       │   ├── web-apps.nix      # Declarative Chromium web apps (YouTube, Apple Music, Nix Search, etc.)
│       │   ├── qutebrowser.nix   # Qutebrowser package + config.py XDG link
│       │   └── scripts.nix       # Wrapped scripts: rebuild, update, idle, menus (wifi/bt/power/audio)
│       ├── laptop/
│       │   ├── default.nix       # Kanata XDG link + power-monitor systemd user service
│       │   └── wallpaper.nix     # swww package + swww-daemon systemd user service
│       └── darwin/
│           └── default.nix       # macOS home dir override, Ghostty + kanata XDG links, macOS packages
│
└── home/ethel/               # Raw dotfiles — linked into XDG config via modules/home/
    ├── niri/               # config.kdl, binds.kdl, input.kdl, layout.kdl, rules.kdl
    ├── ghostty/            # config + shaders/cursor_warp.glsl
    ├── nvim/               # init.lua, lua/ethel/{remap,plugins,highlights,lualine_theme}.lua
    ├── quickshell/         # 13 QML files + scripts/{wifi-monitor,system-stats}.sh
    ├── matugen/            # config.toml + 11 templates (ghostty, mako, wofi, niri, qs, gtk, nvim, etc.)
    ├── wofi/               # config + scripts/{wifi-menu,bluetooth-menu,power-menu,audio-switcher}.sh
    ├── qutebrowser/        # config.py (keybinds, search engines, matugen color sourcing)
    ├── ranger/             # rc.conf, rifle.conf, scope.sh
    ├── kanata.kbd          # Home-row mods + caps→esc/media layer
    ├── scripts/
    │   ├── nixos-rebuild-with-git.sh
    │   ├── power-monitor.sh
    │   └── update.sh
    └── opencode/agents/talk.md
```

---

## Day-to-Day Workflow

### Rebuild

```bash
# Standard
sudo nixos-rebuild switch --flake .#nixos

# With automatic git stage + commit + push
./home/ethel/scripts/nixos-rebuild-with-git.sh
```

macOS:
```bash
darwin-rebuild switch --flake .#<hostname>
```

### Update inputs

```bash
nix flake update
```

### Garbage collect

```bash
clean-nix   # Fish alias — removes old generations and optimizes the store
```

### Format all Nix files

```bash
nix fmt     # runs alejandra on the entire flake
```

### Run checks

```bash
nix flake check                                    # all checks
nix build .#checks.x86_64-linux.<name>             # individual check
```

Available checks: `nixos-build`, `server-build`, `workstation-build`, `server-eval`, `workstation-eval`, `dotfiles-integrity`, `fmt`, `server-vm-test`.

---

## Adding a New Host

1. Create `hosts/<hostname>/` with `hardware.nix` (from `nixos-generate-config`) and `default.nix`.
2. `default.nix` must export `{ system, specialArgs, modules }` as a function of `{ inputs }`.
3. Add `<hostname> = mkNixosHost "<hostname>";` (or `mkDarwinHost`) to `flake.nix`.
4. Choose a profile to import in `modules`; add host-specific overrides inline.
5. Format with `alejandra` and validate with `sudo nixos-rebuild build --flake .#<hostname>`.

---

## Architecture Rules

- **System vs User split is strict.** System daemons and hardware config belong in `modules/system/` or `hosts/`. User apps, dotfiles, and shell config belong in `modules/home/`.
- **No duplication.** Before adding a package or option, confirm it is not already declared.
- **Explicit imports only.** No `*` or catch-all imports. Every module is explicitly listed.
- **XDG linkage.** Dotfiles live in `home/ethel/` and are linked via `xdg.configFile."<name>".source = dotfilesDir + "/<path>";`. Never write config directly into the Nix store unless using the hybrid `runCommand` pattern.
- **`dotfilesDir` specialArg.** All modules receive the `dotfilesDir` path (`../../home/ethel`) via `extraSpecialArgs`. Use it for any `source` or `builtins.readFile` reference.
- **Minimalism first.** Every added package must justify its existence.

---

## Key Subsystems

### Keyboard — Kanata

Home-row mods: `A/S/D/F` act as `Super/Ctrl/Shift/Alt` when held.
Config: `home/ethel/kanata.kbd` | System service: `modules/system/desktop/input.nix`

### Window Manager — Niri

Scrollable-tiling Wayland compositor.
Config: `home/ethel/niri/` | Status bar: `home/ethel/quickshell/` | Launcher: `home/ethel/wofi/`

### Color Theming — Matugen

Wallpaper-based Material Design 3 color generation. A single wallpaper (`~/.config/wallpaper.jpg`) generates colors for 11 applications (Ghostty, mako, wofi, niri, QuickShell, GTK3/4, swaylock, qutebrowser, Neovim).
Config: `home/ethel/matugen/` | Module: `modules/home/desktop/matugen.nix`

### Gaming

Steam + Gamescope + Gamemode.
Module: `modules/system/optional/gaming.nix` (included via the `workstation` profile).

### Laptop Power

power-profiles-daemon + thermald + thinkfan + hibernate via resume-from-swap (LUKS UUID supplied per-host as `swapLuksUuid`).
`modules/system/laptop/power.nix` + `modules/system/laptop/boot.nix`
