# NixOS Dotfiles

A modular, multi-profile NixOS and macOS configuration using **Nix Flakes** and **Home Manager**, following the Suckless philosophy: minimalism, clarity, and explicit configuration over magic defaults.

**Desktop:** Niri (scrollable-tiling Wayland) + Waybar + Wofi + Mako  
**Terminal:** Ghostty | **Editor:** Neovim | **Shell:** Fish  
**Input:** Kanata home-row mods | **Audio:** PipeWire  

---

## Project Structure

```
.
├── flake.nix               # Entry point — mkNixosHost / mkDarwinHost helpers, host registries
├── install.sh              # Interactive bootstrap for a new machine
│
├── profiles/               # Opinionated machine archetypes (import a profile, not individual modules)
│   ├── laptop.nix          # Core + desktop + ThinkPad power management
│   ├── workstation.nix     # Core + desktop + gaming (no power management)
│   ├── server.nix          # Core only — headless, no GUI
│   └── darwin.nix          # nix-darwin + HM: core + desktop + macOS overrides
│
├── hosts/                  # One directory per machine
│   └── nixos/              # Current ThinkPad host
│       ├── default.nix     # { inputs }: { system, specialArgs, modules }
│       └── hardware.nix    # Generated by nixos-generate-config (do not edit)
│
├── modules/
│   ├── system/             # NixOS system modules — one concern per file
│   │   ├── core/
│   │   │   ├── nix.nix         # Flakes, substituters, weekly GC
│   │   │   ├── locale.nix      # Timezone (Kyiv), system language, Ukrainian formats
│   │   │   └── networking.nix  # NetworkManager
│   │   ├── desktop/
│   │   │   ├── audio.nix       # PipeWire
│   │   │   ├── display.nix     # Niri, greetd/tuigreet, fonts
│   │   │   └── input.nix       # Kanata service, uinput udev rules
│   │   ├── laptop/
│   │   │   ├── boot.nix        # systemd-boot, Zen kernel, LUKS (swap partition)
│   │   │   ├── power.nix       # TLP, hibernate, swapLuksUuid
│   │   │   └── services.nix    # ThinkPad-specific services (acpi, etc.)
│   │   └── optional/
│   │       └── gaming.nix      # Steam, Gamemode, kernel tweaks
│   │
│   └── home/               # Home Manager modules — imported by profiles via home-manager.users.ovg.imports
│       ├── core/
│       │   ├── default.nix     # Aggregates shell + neovim + packages; sets home.username / homeDirectory
│       │   ├── shell.nix       # Fish + Bash aliases
│       │   ├── packages.nix    # CLI tools
│       │   └── neovim.nix      # Neovim + Lua config via dotfilesDir
│       ├── desktop/
│       │   ├── default.nix     # Aggregates all desktop HM modules
│       │   ├── theme.nix       # GTK (adw-gtk3-dark), QT (adwaita-dark), color palette
│       │   ├── niri.nix        # Niri config via dotfilesDir
│       │   ├── waybar.nix      # Waybar config + style
│       │   ├── ghostty.nix     # Ghostty config + shaders
│       │   ├── notifications.nix  # Mako
│       │   ├── launcher.nix    # Wofi + shell menu scripts (WiFi, Bluetooth)
│       │   ├── apps.nix        # Zen Browser, Wayland utils, Ghostty, Protontricks
│       │   └── web-apps.nix    # Chromium app-mode entries (YouTube, Apple Music, etc.)
│       ├── laptop/
│       │   └── default.nix     # power-monitor systemd user service + kanata XDG link
│       └── darwin/
│           └── default.nix     # Overrides home.homeDirectory = "/Users/ovg"
│
└── home/ovg/               # Raw dotfiles — linked into XDG config via modules/home/
    ├── niri/               # config.kdl, binds.kdl, input.kdl, layout.kdl, rules.kdl
    ├── ghostty/            # config + shaders/cursor_warp.glsl
    ├── nvim/               # init.lua, lua/ovg/plugins.lua, lua/ovg/remap.lua
    ├── waybar/             # config.jsonc, style.css, scripts/info-box.sh
    ├── wofi/               # config, style.css, scripts/
    ├── mako/config
    ├── ranger/             # rc.conf, rifle.conf, scope.sh
    ├── kanata.kbd
    ├── scripts/
    │   ├── nixos-rebuild-with-git.sh
    │   ├── power-monitor.sh
    │   └── create-note.sh
    └── opencode/agents/talk.md
```

---

## Bootstrapping a New Machine

`install.sh` handles the full setup interactively:

```bash
sudo bash install.sh
```

It will:
1. Detect the platform (Linux or macOS).
2. Prompt for profile (`laptop`, `workstation`, `server`, or auto-selects `darwin`).
3. Prompt for hostname and — for the laptop profile — the swap LUKS UUID.
4. Run `nixos-generate-config` to produce `hosts/<hostname>/hardware.nix`.
5. Write `hosts/<hostname>/default.nix` from the chosen profile template.
6. Inject the new host into `flake.nix` (replaces the `<<NIXOS_HOSTS>>` / `<<DARWIN_HOSTS>>` marker).
7. Set the system hostname.
8. Format the generated file with `alejandra`.
9. Offer to run the initial `nixos-rebuild switch` / `darwin-rebuild switch`.

---

## Day-to-Day Workflow

### Rebuild

```bash
# Standard
sudo nixos-rebuild switch --flake .#nixos

# With automatic git stage + commit + push
./home/ovg/scripts/nixos-rebuild-with-git.sh
```

macOS:
```bash
darwin-rebuild switch --flake .#<hostname>
```

### Update inputs

```bash
nix flake update
```

### Garbage collect

```bash
clean-nix   # Fish alias — removes old generations and optimizes the store
```

### Format all Nix files

```bash
nix fmt     # runs alejandra on the entire flake
```

---

## Adding a New Host

1. Run `install.sh`, **or** manually:
   - Create `hosts/<hostname>/` with `hardware.nix` and `default.nix`.
   - `default.nix` must export `{ system, specialArgs, modules }` as a function of `{ inputs }`.
   - Add `<hostname> = mkNixosHost "<hostname>";` (or `mkDarwinHost`) to `flake.nix`.
2. Choose a profile to import in `modules`; add host-specific overrides inline.
3. Format with `alejandra` and validate with `sudo nixos-rebuild build --flake .#<hostname>`.

---

## Architecture Rules

- **System vs User split is strict.** System daemons and hardware config belong in `modules/system/` or `hosts/`. User apps, dotfiles, and shell config belong in `modules/home/`.
- **No duplication.** Before adding a package or option, confirm it is not already declared.
- **Explicit imports only.** No `*` or catch-all imports. Every module is explicitly listed.
- **XDG linkage.** Dotfiles live in `home/ovg/` and are linked via `xdg.configFile."<name>".source = dotfilesDir + "/<path>";`. Never write config directly into the Nix store.
- **`dotfilesDir` specialArg.** All modules receive the `dotfilesDir` path (`../../home/ovg`) via `extraSpecialArgs`. Use it for any `source` or `builtins.readFile` reference.
- **Minimalism first.** Every added package must justify its existence.

---

## Key Subsystems

### Keyboard — Kanata

Home-row mods: `A/S/D/F` act as `Super/Ctrl/Shift/Alt` when held.  
Config: `home/ovg/kanata.kbd` | System service: `modules/system/desktop/input.nix`

### Window Manager — Niri

Scrollable-tiling Wayland compositor.  
Config: `home/ovg/niri/` | Status bar: `home/ovg/waybar/` | Launcher: `home/ovg/wofi/`

### Gaming

Steam + Gamescope + Gamemode + Lutris + Protontricks.  
Module: `modules/system/optional/gaming.nix` (included via the `workstation` profile).  
Zen kernel and tuned kernel parameters in `modules/system/laptop/boot.nix`.

### Laptop Power

TLP + hibernate via resume-from-swap (LUKS UUID supplied per-host as `swapLuksUuid`).  
`modules/system/laptop/power.nix` + `modules/system/laptop/boot.nix`
