{
    "layer": "top",
    "position": "top",
    "height": 30,
    "modules-left": [ "custom/cpu", "custom/memory", "niri/workspaces", "niri/window" ],
    "modules-center": [ "clock" ],
    "modules-right": [ "tray", "niri/language", "network", "bluetooth", "battery" ],

    "niri/workspaces": {
        "format": "{name}"
    },

    "niri/window": {
        "format": "{}",
        "max-length": 50,
        "separate-outputs": true
    },

    "network": {
        "format-wifi": "",
        "format-ethernet": "󰈀",
        "format-disconnected": "󰖪",
        "tooltip-format": "{ifname} via {gwaddr} 󰊗",
        "tooltip-format-wifi": " {essid} ({signalStrength}%)",
        "tooltip-format-ethernet": "󰈀 {ifname} ",
        "tooltip-format-disconnected": "Disconnected",
        "on-click": "wifi-menu",
        "max-length": 50
    },

    "bluetooth": {
        "format": "",
        "format-disabled": "󰂲", // an empty format will hide the module
        "format-connected": "",
        "tooltip-format": "{controller_alias}\t{controller_address}",
        "tooltip-format-connected": "{controller_alias}\t{controller_address}\n\n{device_enumerate}",
        "tooltip-format-enumerate-connected": "{device_alias}\t{device_address}",
        "on-click": "bluetooth-menu"
    },

    "clock": {
        "format": "{:%H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format-alt": "{:%Y-%m-%d}"
    },

    "custom/cpu": {
        "exec": "~/.config/waybar/scripts/system-stats.py cpu",
        "return-type": "json",
        "interval": 1,
        "format": "{}",
        "tooltip": true
    },

    "custom/memory": {
        "exec": "~/.config/waybar/scripts/system-stats.py ram",
        "return-type": "json",
        "interval": 1,
        "format": "{}",
        "tooltip": true
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": "󰂄 {capacity}%",
        "format-plugged": " {capacity}%",
        "format-alt": "{icon} {time}",
        "format-icons": ["", "", "", "", ""]
    },

    "tray": {
        "spacing": 10,
        "ignored-list": [
            "nm-applet",
            "blueman-applet"
        ]
    },

    "niri/language": {
        "format": " {}"
    }
}
